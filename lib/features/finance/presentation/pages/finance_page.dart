import 'package:flutter/material.dart';

class FinancePage extends StatelessWidget {
  const FinancePage({super.key});

  static const String title = 'Finance';

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Finance'),
    );
  }
}
