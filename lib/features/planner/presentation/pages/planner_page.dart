import 'package:flutter/material.dart';

class PlannerPage extends StatelessWidget {
  const PlannerPage({super.key});

  static const String title = 'Planner';

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Planner'),
    );
  }
}
